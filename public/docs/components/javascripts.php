<section id="javascript">
    <h2>JavaScript</h2>
    <p class="mb-0"><code>function selectInput</code> maakt een hele tabel row klikbaar. Zo hoef je niet percies op de radio knop te klikken, maar kan je ook op de witruimite ernaast klikken. <code>function pagination</code> maakt verschillende <code>div</code> elementen <code>display: none</code> of <code>display: block</code>. Op deze manier hebben wij pagination zonder van <i>pagina</i> te wisselen</p>

    <pre>
        <code class="php">
<?php import('../js/scripts.js') ?>
        </code>
    </pre>
</section>